CREATE TABLE person 
(
    id long NOT NULL AUTO_INCREMENT,
    name VARCHAR(128),
	email VARCHAR(128),
	mobile_number VARCHAR(128),
    PRIMARY KEY (id)
);

CREATE TABLE animal 
(
    id long NOT NULL AUTO_INCREMENT,
    name VARCHAR(128),
	email VARCHAR(128),
	mobile_number VARCHAR(128),
    PRIMARY KEY (id)
);